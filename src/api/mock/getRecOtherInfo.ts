import { RecOtherInfo } from '@/types'

export default (disciplineId: number, studentGroupId: number): RecOtherInfo => ({
  students_group_type: 'students group type',
  students_discipline_semester: 2,
  request_count: 4,
  students_group_count: 20,
})
